# Docker Compose - PostgreSQL + API
# aegra dev  -> docker compose up postgres -d  (database only)
# aegra up   -> docker compose up --build      (full stack)

services:
  postgres:
    image: pgvector/pgvector:pg18
    container_name: sandbox-agent-postgres
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-sandbox_agent}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-sandbox_agent_secret}
      POSTGRES_DB: ${POSTGRES_DB:-sandbox_agent}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-sandbox_agent}"]
      interval: 5s
      timeout: 5s
      retries: 5

  sandbox_agent:
    build: .
    container_name: sandbox-agent-api
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-sandbox_agent}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-sandbox_agent_secret}
      - POSTGRES_HOST=postgres
      - POSTGRES_DB=${POSTGRES_DB:-sandbox_agent}
      - AEGRA_CONFIG=langgraph.json
      - AUTH_TYPE=${AUTH_TYPE:-noop}
      - PORT=${PORT:-8000}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      start_period: 10s
    volumes:
      - ./src:/app/src:ro
      - ./langgraph.json:/app/langgraph.json:ro

volumes:
  postgres_data:
